import { ContentCreatorGuard } from './content-creator.guard';

describe('ContentCreatorGuard', () => {
  it('should be defined', () => {
    expect(new ContentCreatorGuard()).toBeDefined();
  });
});
